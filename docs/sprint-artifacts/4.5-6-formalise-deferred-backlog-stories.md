# Story 4.5-6: Formalise Deferred Backlog Stories

**Status:** done  
**Epic:** 4.5 — Retrospective Debt Resolution  
**Origin:** Epics 2–4 retrospectives — 4 named-but-unscheduled items

---

## Story

**As a** developer planning upcoming epics,  
**I want** every named improvement from past retrospectives to have a formal entry in `sprint-status.yaml`,  
**So that** deferred work is tracked as a commitment instead of a comment that will be forgotten.

---

## Background

### The Problem

Four improvement items received names in retrospectives or story notes but were never added to `sprint-status.yaml`. Without a backlog entry, an item exists only as a prose comment. It has no status, no home epic, and no way to be picked up during sprint planning.

### The Four Items

| Item | Named In | Current State | Recommended Home |
|------|----------|---------------|-----------------|
| OAuth edge cases: cancel mid-flow (F4), dismiss sheet (F5), expired redirect token (F6) | Epic 4 Story 4.4-2 dev notes | Comment only — never entered in tracking | Epic 7 (Polish) |
| Image hosting / thumbnail uploads for listings | Epic 3 retro + multiple story notes | "Epic 6" referenced in prose but no entry | Epic 5 (Listing Creation) |
| Desktop sidebar collapse/expand toggle for the workstation rail | Epic 3 retrospective | Not tracked anywhere | Epic 6 (Platform) |
| Offline migration retry queue | Epic 4 retrospective | Not tracked anywhere | Epic 6 (Platform) |

### What Already Exists

| Item | Location | Notes |
|---|---|---|
| `sprint-status.yaml` | `docs/sprint-artifacts/sprint-status.yaml` | Contains Epics 0–7; Epic 5 has 10 stories, Epic 6 and 7 have placeholder entries |
| Epic 5 stories | `sprint-status.yaml` lines ~122–135 | `5-1` through `5-10` all at `backlog` |
| Epic 6 stories | `sprint-status.yaml` | `6-1` through `6-10` all at `backlog` |
| Epic 7 stories | `sprint-status.yaml` | `7-1` through `7-12` all at `backlog` |

### What This Story Delivers

**One file modified:** `docs/sprint-artifacts/sprint-status.yaml`

Four new backlog entries added under the appropriate epics — each with a comment noting its retrospective origin.

---

## Acceptance Criteria

### AC1: All Four Items in sprint-status.yaml

**Given** `sprint-status.yaml` is read  
**When** each of the four deferred items is searched for  
**Then** each has a story-key entry (e.g., `5-11-image-hosting-thumbnails: backlog`) under its assigned epic

### AC2: Origin Comments Present

**Given** the four new entries  
**When** each line is read  
**Then** each has an inline comment (`# Origin: ...`) identifying which retrospective or story it came from

### AC3: Entries Are in the Correct Epic Section

**Given** the YAML structure  
**When** the entries are read  
**Then**:
- OAuth edge cases entry is under `# Epic 7` section
- Image hosting entry is under `# Epic 5` section
- Workstation rail collapse entry is under `# Epic 6` section
- Offline migration retry queue entry is under `# Epic 6` section

### AC4: Existing Entries Are Not Modified

**Given** the existing `sprint-status.yaml` content  
**When** the four new entries are added  
**Then** all previously existing epic, story, and retrospective entries are unchanged

### AC5: No Prose-Only Deferred Items Remain

**Given** the four items identified in the debt inventory  
**When** `sprint-status.yaml` is read  
**Then** every item from the table above has a formal backlog entry (none remain as comments-only)

---

## Dev Notes

### Exact Sprint-Status.yaml Additions

Read the current `sprint-status.yaml` to find the correct insertion points for each epic section. Then add the following entries:

**Under `# Epic 5` section** (after the last `5-*` story entry, before `epic-5-retrospective` if it exists):
```yaml
  5-11-image-hosting-thumbnails: backlog  # Origin: Epic 3 retro + Story 3.4 notes; listings need photo upload
```

**Under `# Epic 6` section** (after the last `6-*` story entry, before `epic-6-retrospective`):
```yaml
  6-11-desktop-sidebar-collapse: backlog  # Origin: Epic 3 retro; desktop rail has no collapse/expand toggle under the 10/45/45 workstation model
  6-12-offline-migration-retry-queue: backlog  # Origin: Epic 4 retro; failed guest migration not retried when back online
```

**Under `# Epic 7` section** (after the last `7-*` story entry, before `epic-7-retrospective`):
```yaml
  7-13-oauth-edge-cases-cancel-dismiss-expired: backlog  # Origin: Epic 4 Story 4.4-2 dev notes; F4/F5/F6 paths untested
```

### How to Read the Current State Before Making Changes

Before modifying the file, read `docs/sprint-artifacts/sprint-status.yaml` completely to:
1. Find the highest existing story number in each of Epics 5, 6, and 7
2. Determine if Epic 6 and Epic 7 already have story entries (they may only have `epic-6: backlog`)
3. Assign the correct sequential story numbers for the new entries

### Numbering Rules

- Epic 5: Current stories are `5-1` through `5-10`. New entry = `5-11`.
- Epic 6: Current stories are `6-1` through `6-10`. New entries = `6-11`, `6-12`.
- Epic 7: Current stories are `7-1` through `7-12`. New entry = `7-13`.

### Scope of This Story

This story is **only** about adding the four entries to `sprint-status.yaml`. It does NOT involve:
- Creating story artifact `.md` files for the four new items (that happens when those epics are planned)
- Modifying retrospective documents
- Changing any existing sprint-status entries
- Making any code changes

### References

- Sprint status file: [docs/sprint-artifacts/sprint-status.yaml](./sprint-status.yaml)
- Epic 3 retrospective (image hosting + sidebar): [docs/sprint-artifacts/epic-3-retrospective.md](./epic-3-retrospective.md)
- Epic 4 retrospective (OAuth edge cases, offline migration): [docs/sprint-artifacts/epic-4-retrospective.md](./epic-4-retrospective.md)
- Story 4.4-2 dev notes (OAuth F4/F5/F6): [docs/sprint-artifacts/4-4-implement-google-oauth-sign-in.md](./4-4-implement-google-oauth-sign-in.md)

---

## Dev Agent Record

### Agent Model Used

GPT-5.4

### Debug Log References

- Workspace validation only; no separate debug log artifact was persisted for this story.

### Completion Notes List

- Added four deferred backlog entries to `sprint-status.yaml` under the correct epic sections with sequential numbering.
- Preserved the existing tracking structure and added inline origin comments for each new item.
- Follow-up review cleanup completed the story audit metadata; no implementation changes were required beyond the already-correct backlog entries.

### File List

- `docs/sprint-artifacts/sprint-status.yaml`

## Senior Developer Review (AI)

**Date:** 2026-04-03  
**Outcome:** Approved

- Verified all four deferred items now exist as formal backlog entries under the intended epics.
- Confirmed numbering, placement, and origin comments match the story requirements.
- Populated the Dev Agent Record so the story no longer lacks completion evidence.

## Change Log

- 2026-04-03: Completed story audit metadata after review verification.
