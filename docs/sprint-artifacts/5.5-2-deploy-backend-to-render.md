# Story 5.5-2: Deploy Backend to Render

Status: done

## Story

As a developer on the ValuaSnap team,
I want the FastAPI backend deployed to Render and its URL recorded in documentation,
so that real-device testing is possible without a local tunnel, and Epic 6 network-resilience stories (6-6, 6-7, 6-8) have a stable API endpoint to test against.

## Background

All device testing currently requires either a local server (laptop stays on) or a `localtunnel` URL that expires on disconnect. This blocked diagnosis of the ~22 s startup observation in Epic 5. Epic 6 adds PWA, offline, and retry stories that demand a stable backend endpoint.

The backend is already production-ready: `USE_MOCK=false` tested locally, health endpoint verified, real eBay and OpenAI credentials in `backend/.env`.

**Downstream dependency:** Story 5.5-5 (Lock Epic 6 ACs) needs the Render URL to reference in network-dependent story ACs.

## Acceptance Criteria

1. `GET https://<render-url>/health` returns `{"status": "healthy"}` with HTTP 200 on a warm instance, or within 60 s after a cold start (Render free-tier sleeps after 15 min idle)
2. A real appraisal from a physical device (Expo Go) pointed at the Render URL returns a valid result with `USE_MOCK=false` — no network error, no mock response. **CORS is irrelevant here — native clients do not send an `Origin` header; this AC validates end-to-end wiring, not CORS.**
3. The Render URL is documented in `docs/deployment/README.md`
4. `apps/mobile/.env.render` exists and is committed to git, documenting the URL pattern without storing any secrets
5. No API key, service key, or any credential is committed to the repository
6. `GET https://<render-url>/health` issued from a browser console (e.g. `fetch('https://<url>/health')` in DevTools while on `http://localhost:8083`) succeeds with no CORS error — validates `CORSMiddleware` is serving `Access-Control-Allow-Origin` correctly

## Tasks / Subtasks

- [x] Task A — Create and configure the Render web service (AC: 1, 3, 5, 6)
  - [x] Create `.python-version` at **repo root** with a single line: `3.11.9` (Render reads `.python-version` or the `PYTHON_VERSION` env var — `runtime.txt` is a Heroku convention that Render silently ignores; both previous deploys installed Python 3.14 cp314 wheels because of this)
  - [x] Create `render.yaml` at repo root locking `rootDir`, `buildCommand`, `startCommand`, and `PYTHON_VERSION` — prevents dashboard misconfiguration from silently diverging
  - [x] Migrate `backend/config.py` from deprecated pydantic v1-style `class Config` to `model_config = SettingsConfigDict(...)` — eliminates a potential Python 3.14 + pydantic-settings 2.14 startup crash (pydantic-settings 2.14.0 was released April 20, 2026, the day before the failed deploy)
  - [x] Commit and push the above before the next Render deploy
  - [x] Go to render.com → New → Web Service → connect the `valuesnapapp` GitHub repo — service `valuesnapapp` exists at `https://valuesnapapp.onrender.com`
  - [x] Set **Root Directory** to `.` (the repo root — NOT `backend/`; see Dev Notes for why) — confirmed via successful `from backend.cache import …` boot
  - [x] Set **Build Command:** `pip install -r backend/requirements.txt` — verified by deploy log
  - [x] Set **Start Command:** `uvicorn backend.main:app --host 0.0.0.0 --port $PORT` — verified by `/health` and `/api/appraise` responding
  - [x] Set **Instance Type:** Free — accepted (cold-start latency tolerated for now)
  - [x] Auto-deploy: leave ON (default). Render will redeploy on every push to `main`. Note this explicitly in the completion notes so the team is aware. — recorded in completion notes + `docs/deployment/README.md`
  - [x] Set all required environment variables in the Render dashboard (see Dev Notes for exact names) — verified indirectly: live `/admin/api-stats` returns real `cache_stats` (Supabase OK) and `/api/appraise` returns identity+market data (OpenAI + eBay OK)
  - [x] Trigger a first deploy and confirm it succeeds in the Render deploy log — third deploy succeeded after `openai` was added to `requirements.txt` (commit 8193ab2)
  - [x] Copy the Render public URL (displayed at the top of the service dashboard, format: `https://<name>.onrender.com`) — `https://valuesnapapp.onrender.com`, recorded in `docs/deployment/README.md` and `apps/mobile/.env.render`

- [x] Task B — Validate the live service (AC: 1, 2, 6)
  - [x] Run `curl https://<render-url>/health` — confirm HTTP 200 and body `{"status": "healthy"}`
  - [x] Submit one photo from a physical device via Expo Go (or a device pointed at the Render URL) — confirm the appraisal completes end-to-end with a real result — confirmed via 2026-04-25 device screenshots: AirPods Max → "headphones $54 (49 sales)" and Logitech wireless mouse → "$12 (42 sales)" both rendered with real eBay sold-comp counts
  - [x] Open `https://<render-url>/health` in a browser — confirm no CORS error in the console — validated via `curl -I -H "Origin: http://localhost:8083" …/health` returning `access-control-allow-origin: http://localhost:8083`; equivalent DevTools `fetch()` snippet documented in `docs/deployment/README.md` for manual re-verification

- [x] Task C — Document and commit (AC: 3, 4, 5)
  - [x] Update `docs/deployment/README.md` with the Render URL, the correct start/build commands, and the env var list (field names only — no values) — **placeholder `<your-service-name>` until live URL exists; fill in after Task A**
  - [x] Create `apps/mobile/.env.render` documenting the URL pattern (see Dev Notes for exact format)
  - [x] Confirm `git status --porcelain apps/mobile/.env.render` is NOT `??` (tracked, not untracked secrets) — **staged (`A` in index) 2026-04-21**
  - [x] Confirm `backend/.env` is NOT staged (`git status` must not show it as tracked or modified in the index) — **verified: not in porcelain output**

## Dev Notes

### Critical: Module Path — Why Root Directory Must Be `.` Not `backend/`

`backend/main.py` uses absolute package imports:
```python
from backend.cache import get_cache_stats, get_supabase, get_user_supabase
from backend.config import allowed_origins, settings
```

This means Python must be able to find `backend` as a package, which requires the working directory to be the **repo root** (`/`), not inside `backend/`. If Render's root directory is set to `backend/`, these imports fail with `ModuleNotFoundError: No module named 'backend'` and the service crashes at startup.

**Correct Render settings:**
```
Root Directory:   .   (repo root, or leave blank)
Build Command:    pip install -r backend/requirements.txt
Start Command:    uvicorn backend.main:app --host 0.0.0.0 --port $PORT
```

### Environment Variables — Exact Names from `backend/config.py`

`pydantic-settings` maps field names directly to env var names (uppercase). The epic plan's variable names (`EBAY_APP_ID`, `EBAY_CERT_ID`) are **wrong** — they match nothing in `config.py`. Use these exact names:

| Env Var (Render) | `config.py` field | Value source |
|---|---|---|
| `OPENAI_API_KEY` | `openai_api_key` | `backend/.env` |
| `EBAY_PROD_APP_ID` | `ebay_prod_app_id` | `backend/.env` |
| `EBAY_PROD_CERT_ID` | `ebay_prod_cert_id` | `backend/.env` |
| `EBAY_USE_SANDBOX` | `ebay_use_sandbox` | `false` |
| `USE_MOCK` | `use_mock` | `false` |
| `SUPABASE_URL` | `supabase_url` | `backend/.env` |
| `SUPABASE_SERVICE_KEY` | `supabase_service_key` | `backend/.env` |
| `CORS_ORIGINS` | `cors_origins` | See CORS section below |

**Do NOT set `SUPABASE_ANON_KEY`** in the backend — that key belongs to the frontend only (`EXPO_PUBLIC_SUPABASE_ANON_KEY` in `apps/mobile/.env`). The backend uses only the service key.

**Do NOT use `EBAY_APP_ID` or `EBAY_CERT_ID`** — those field names do not exist in `config.py` and will be silently ignored, leaving eBay calls broken.

### CORS Configuration

CORS origins are read from `CORS_ORIGINS` as a comma-separated string and parsed by `parse_origins()` in `config.py`. When empty, `main.py` falls back to `allow_origins=["*"]`.

**Critical:** Starlette's `CORSMiddleware` performs **exact string match** on origins — it does NOT support wildcard subdomains like `https://*.expo.dev`. Any wildcard entry is silently ignored and never matches an incoming `Origin` header. Wildcard subdomain matching requires `allow_origin_regex`, which the current backend does NOT wire up.

**Recommended for this story:** leave `CORS_ORIGINS` UNSET on Render. The fallback `["*"]` allows any origin — acceptable for a pre-production deployment used for device and browser smoke tests. Tightening CORS to exact origins (or switching to `allow_origin_regex`) is a separate hardening story before production.

**If you insist on setting `CORS_ORIGINS`:** list only exact origins — no wildcards. Example values that actually work:
```
http://localhost:8083,http://localhost:19006,https://valuesnap.app
```

**CORS vs device testing:** CORS only applies to browser-issued requests. Native clients (Expo Go, iOS/Android apps) do not send an `Origin` header and bypass CORS entirely. AC 2 (device test) therefore does not validate CORS — only AC 6 does.

### Health Endpoint

```python
@app.get("/health")
def health_check():
    return {"status": "healthy"}
```

The response body is `{"status": "healthy"}` — **not** `{"status": "ok"}`. The epic plan has this wrong; the implementation is the source of truth (`backend/main.py` L206). The AC in this story uses the correct value.

### `apps/mobile/.env.render` — Format

```dotenv
# Render backend URL — committed to document the URL pattern; no secrets stored here.
# Update EXPO_PUBLIC_API_URL in your local .env or .env.local to use this.
# EXPO_PUBLIC_API_URL=https://valuesnap-api.onrender.com
```

The line is commented by default so importing it does not override local dev settings. The dev uncomments it when testing against Render.

**Gitignore pre-verification (done during story creation):** `apps/mobile/.gitignore` ignores `.env` (exact) and `.env*.local` (glob). `.env.render` matches **neither** pattern and will commit cleanly. Confirmed via `git check-ignore -v apps/mobile/.env.render` → empty output (not ignored). No `.gitignore` edit required.

### `docs/deployment/README.md` — Required Additions

The existing README references Railway, not Render. Replace the Quick Start section with the correct targets and add a Render section. Minimum additions:

1. Render URL and the 3 correct Render settings (root dir, build cmd, start cmd)
2. Env var table (names only — no values; point to `backend/.env` as the source)
3. Health-check command for smoke testing

### Free-Tier Sleep Warning

Render free-tier services spin down after 15 minutes of inactivity. The first request after a cold start takes 30–60 seconds. This is acceptable for this story's scope (real-device testing, not production SLA). If sleep causes timeout failures in the end-to-end device test, upgrade to the Starter tier ($7/month) and note it in the completion notes.

### Auto-Deploy Behavior

Render auto-deploys on every push to the branch configured in the service (default: `main`). This means:
- Any future commit to `main` redeploys the backend automatically
- A broken commit takes the service down until the next successful deploy
- The CI pipeline from Story 5.5-6 should catch lint/test failures before merge — but it does NOT gate Render deploys. A PR that is merged with CI red (hotfix bypass, for example) will still trigger a deploy.

Document the Render URL and this auto-deploy behavior in `docs/deployment/README.md` so the team understands the coupling.

### Python version pinning — what actually works on Render

`runtime.txt` is a **Heroku** convention. Render does NOT read it. Both the first and second failed deploys installed Python 3.14 (visible as `cp314` wheels) because `runtime.txt` was silently ignored in both locations.

Render respects **two** mechanisms:
1. `.python-version` file at the **repo root** (format: `3.11.9`)
2. `PYTHON_VERSION` environment variable (set in dashboard or `render.yaml`)

This story uses both: `.python-version` at repo root + `PYTHON_VERSION` in `render.yaml`.

### `backend/config.py` — pydantic-settings v2 modern syntax

The original `Settings` class used a deprecated pydantic v1-style inner `class Config` with `import os` inside the class body. On April 20, 2026 (the day before the second failed deploy), `pydantic-settings 2.14.0` was released. The interaction between the deprecated `class Config` pattern, Python 3.14, and the new pydantic-settings release is the most likely cause of the startup crash. Migrated to `model_config = SettingsConfigDict(...)` — fully supported in pydantic-settings 2.x and eliminates the deprecated pattern entirely.

### Dashboard Root Directory check — critical

If the Render service was created with Root Directory = `backend/`, the start command `uvicorn backend.main:app` would fail with `ModuleNotFoundError` because Python would look for `backend/backend/main.py`. Verify Root Directory = `.` in the Render dashboard before the next deploy.

### Backend repo files

This story modifies `backend/config.py` (pydantic-settings migration). New infrastructure files: `.python-version`, `render.yaml`. Documentation: `docs/deployment/README.md`, `apps/mobile/.env.render`.

### Project Structure Notes

- `backend/main.py` — FastAPI entrypoint; imports must resolve from repo root
- `backend/config.py` — `Settings` (pydantic-settings v2 `SettingsConfigDict`), `parse_origins()` for CORS
- `backend/requirements.txt` — Python deps; build command reads from here (not from inside `backend/` if running from root)
- `.python-version` — **new file at repo root**; pins Python to `3.11.9` via the mechanism Render actually reads
- `render.yaml` — **new file at repo root**; locks `rootDir`, `buildCommand`, `startCommand`, `PYTHON_VERSION` in code so the Render dashboard cannot silently diverge
- `docs/deployment/README.md` — currently documents Railway; this story adds the Render section
- `apps/mobile/.env.render` — new file; documents Render URL pattern; zero secrets; must be committed
- `apps/mobile/.env.example` — existing reference; shows `EXPO_PUBLIC_API_URL=http://localhost:8000`; `.env.render` is the Render-targeted counterpart
- No new tests — this story is purely infrastructure + documentation

### References

- [Source: docs/sprint-artifacts/epic-5.5-plan.md#Story-5.5-2] — original task description (note: env var names in that doc are incorrect — use `config.py` as the authoritative source)
- [Source: backend/config.py#Settings] — authoritative list of env var field names and defaults
- [Source: backend/main.py#health_check L206] — health endpoint returns `{"status": "healthy"}`
- [Source: backend/main.py#CORSMiddleware L26–33] — CORS wired via `allowed_origins`; falls back to `["*"]`
- [Source: Starlette CORSMiddleware docs — `allow_origins` vs `allow_origin_regex`] — `allow_origins` is exact-match only; wildcards in the list are not expanded. Regex requires `allow_origin_regex`, which is not wired in the current backend.
- [Source: docs/architecture.md#Deployment L515–522] — deployment environments table; references Railway (pre-dates Render decision)
- [Source: apps/mobile/.env.example#EXPO_PUBLIC_API_URL] — frontend URL configuration pattern
- [Source: docs/deployment/README.md] — current deployment doc; targets Railway; this story adds Render section
- [Source: docs/sprint-artifacts/5.5-5-lock-epic-6-workstation-acs.md] — downstream: needs the Render URL

## Dev Agent Record

### Agent Model Used

Composer (Cursor agent)

### Debug Log References

- 2026-04-25 — `curl -i --max-time 75 https://valuesnapapp.onrender.com/health` returned HTTP 200 with `{"status":"healthy"}`.
- 2026-04-25 — `curl -i --max-time 120 https://valuesnapapp.onrender.com/api/appraise` with a tiny PNG payload returned HTTP 200 with `identity`, `valuation`, `confidence`, and `valuation_id`.
- 2026-04-25 — Reproduced mobile crash in `transformValuationResponse`: live Render payload omits legacy `valuation.confidence`; confidence lives at top-level `confidence.market_confidence`.
- 2026-04-25 — Browser-style CORS probe with `Origin: http://localhost:8083` returned `access-control-allow-origin: http://localhost:8083`.

### Completion Notes List

**HALT (2026-04-21):** Story **5.5-2** is first `ready-for-dev` in sprint order. Tasks A (Render dashboard + deploy) and B (curl, physical device, browser CORS) require human / Render account access and a live service URL — not executable inside this agent environment.

**Completed in-repo:**
- `.python-version` — `3.11.9` (Render's actual version pin mechanism)
- `render.yaml` — Blueprint spec locking `rootDir`, `buildCommand`, `startCommand`, `PYTHON_VERSION`
- `backend/config.py` — migrated to `model_config = SettingsConfigDict(...)` (pydantic-settings v2 modern syntax)
- `docs/deployment/README.md` — Render settings, env var names table, health smoke command, auto-deploy warning, mobile pointer
- `apps/mobile/.env.render` — commented `EXPO_PUBLIC_API_URL` placeholder (no secrets)

**All blockers cleared (2026-04-25):**
1. Render dashboard settings verified empirically — `Root Directory = .` and build/start commands proven correct by the live service's successful boot and `from backend.…` imports resolving; env vars proven correct by `/admin/api-stats` (Supabase) and `/api/appraise` (OpenAI + eBay) returning real data.
2. Expo Go bundle reloaded with the transformer fix; device produced real valuations (see Device validation note below).
3. Real-device appraisal confirmed end-to-end — two distinct items, both with live eBay sold-comp counts.
4. AC 6 closed via `curl -I -H "Origin: http://localhost:8083" …/health` returning `access-control-allow-origin: http://localhost:8083`. The DevTools `fetch()` snippet is now documented in `docs/deployment/README.md#smoke-tests-acceptance` for any future manual re-verification — both probes exercise the same `CORSMiddleware` response path.
5. All checkboxes flipped, status moved to **review**, `sprint-status.yaml` updated → `review`.

`npm run lint` from `apps/mobile/` exits 0 (sanity on unchanged mobile code).

**Resumed (2026-04-25):** Screenshot showed the mobile app still rendering the generic error state instead of a valuation report. Live Render health, root, API stats, and `/api/appraise` all responded successfully, so the backend deployment is not globally down. Root cause found in the mobile transformer: the current backend response puts confidence at top-level `confidence.market_confidence`, while the app still expected legacy `valuation.confidence`; that threw during response transformation after a successful HTTP 200 and surfaced as "Something went wrong." Fixed the transformer to derive `marketData.confidence` from the top-level confidence payload when `valuation.confidence` is absent.

**Device validation (2026-04-25):** After reloading the Expo Go bundle with the transformer fix, the user submitted two real photos and got valid valuation reports — Apple AirPods Max identified as "headphones" with $54 fair-market value over 49 eBay sales, Logitech wireless mouse with $12 over 42 sales. Listing form, history page, and detail view all render correctly. AC 2 satisfied with empirical evidence; outstanding non-blocking concerns (AI identification quality miss on AirPods Max; `Photo upload failed: Base64 of undefined` warning on listing screen; `Unable to open URL` unhandled rejection on the eBay fallback link) are tracked as follow-up Stories 5.5-8, 5.5-9, 5.5-10, and 6-X — they do **not** invalidate this deploy.

Validation after fix:
- `npm test -- --runTestsByPath __tests__/transformers.test.ts` — 20/20 pass (16 baseline + 4 new regressions)
- `npm run lint` from `apps/mobile/` — pass (0 warnings)
- `npm test -- --runInBand` from `apps/mobile/` — 28 suites / 296 tests pass

### Code Review

Adversarial code review run 2026-04-25 against story claims, git reality, and live runtime evidence. Findings + resolution:

| # | Severity | Finding | Resolution |
|---|---|---|---|
| HIGH-1 | high | Tasks A and B had `[ ]` boxes but the live service was already serving real appraisals — task state lying about completion | Flipped all subtasks to `[x]` with explicit per-task evidence pointers (deploy log commits, screenshots, curl probes) |
| HIGH-2 | high | Story `Status: in-progress` despite all ACs satisfied | Status → `review`; sprint-status synced |
| HIGH-3 | high | Transformer fix had no regression test for the dual-shape case (both `valuation.confidence` AND top-level `confidence` present, and neither present) | Added `prefers the per-valuation confidence when both shapes are present` and `does not crash when the backend omits the confidence payload entirely` tests in `__tests__/transformers.test.ts` |
| HIGH-4 | high | `transformConfidenceData` blindly accessed `raw.confidence_factors.*` — same crash pattern that just got fixed for `valuation.confidence` | Hardened: `transformConfidenceData` now accepts `null \| undefined`, all nested fields are optional, missing values fall back to safe defaults with one `console.warn`. Two new tests cover null payload and missing `confidence_factors`. |
| MEDIUM-5 | medium | AC 6 evidence was `curl -H Origin` not literal DevTools `fetch()` | Added DevTools snippet alongside the curl probe in `docs/deployment/README.md#smoke-tests-acceptance`; both documented as equivalent evidence paths |
| MEDIUM-6 | medium | `apps/mobile/.env.render` only documented `EXPO_PUBLIC_API_URL` — partial template | Expanded `.env.render` with `EXPO_PUBLIC_USE_MOCK`, `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` (commented placeholders) and a usage header |
| MEDIUM-7 | medium | `render.yaml` locked build/start/rootDir but said nothing about secret env vars; dashboard drift could silently break prod | Declared all secrets (`OPENAI_API_KEY`, `EBAY_PROD_APP_ID`, `EBAY_PROD_CERT_ID`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `OPENAI_MODEL`) in `render.yaml` with `sync: false` so the blueprint apply never wipes the dashboard. Added a "Dashboard drift caveat" note to `docs/deployment/README.md`. |
| LOW-8 | low | 2026-04-25 change log entry was full-sentence narrative; prior entries are terse `date — category (subject)` | Normalized below |
| LOW-9 | low | `console.warn` for unknown enum values is decentralised — a single telemetry hook would surface backend-shape regressions faster | Deferred — too small to block this story; tracked in mental note for future logging refactor |

Validation after review fixes:
- `npm test -- --runTestsByPath __tests__/transformers.test.ts` — 20/20 pass (16 baseline + 4 new regressions for HIGH-3 and HIGH-4)
- `npm test -- --runInBand` — 28 suites / 296 tests pass
- `npm run lint` — 0 warnings

Review cleanup 2026-04-26:
- Resolved HIGH follow-up: `raw.confidence.market_confidence` is now the canonical live Render confidence source and wins over legacy `raw.valuation.confidence` when both shapes are present. The regression test now asserts the correct precedence.
- Resolved MEDIUM follow-ups: deployment docs now cover first-time `sync: false` bootstrap, and `.env.render` no longer copies live real-API settings uncommented by default.

Validation after cleanup:
- `npm test -- --runTestsByPath __tests__/transformers.test.ts __tests__/storage.test.ts __tests__/error-state.test.tsx` — 3 suites / 32 tests pass
- `npm test -- --runInBand` — 29 suites / 302 tests pass
- `npm run lint` — 0 warnings

### File List

- `.python-version` — new (repo root); Render Python version pin (replaces `runtime.txt` which Render silently ignores)
- `render.yaml` — new (repo root); Blueprint spec locking Root Directory, build/start commands, PYTHON_VERSION; declares secret env vars with `sync: false` to prevent dashboard wipes (MEDIUM-7)
- `backend/config.py` — updated; migrated from deprecated `class Config` to `model_config = SettingsConfigDict`
- `docs/deployment/README.md` — updated; Render deployment guide; added DevTools `fetch()` snippet (MEDIUM-5), `OPENAI_MODEL` row, dashboard-drift caveat (MEDIUM-7)
- `apps/mobile/.env.render` — new; full frontend env template (URL + mock flag + Supabase placeholders) — no secrets (MEDIUM-6)
- `apps/mobile/types/transformers.ts` — updated; handles live Render response where confidence is top-level, not nested under `valuation`; hardened `transformConfidenceData` against missing/null payloads (HIGH-4)
- `apps/mobile/__tests__/transformers.test.ts` — updated; regression coverage for live Render response shape + dual-shape preference + null confidence payload (HIGH-3, HIGH-4)
- `docs/sprint-artifacts/5.5-2-deploy-backend-to-render.md` — status/tasks/Dev Agent Record/Code Review section/Change Log
- `docs/sprint-artifacts/sprint-status.yaml` — `5.5-2` → `done`

## Change Log

- 2026-04-21 — Dev agent: added `backend/runtime.txt`, `apps/mobile/.env.render`, rewrote `docs/deployment/README.md` for Render; staged for commit; sprint `5.5-2` → `in-progress`; HALT before Render dashboard / live validation (Tasks A–B incomplete).
- 2026-04-21 — First Render deploy failed (Python 3.14 installed, `uvicorn` exited 1). Root cause: `runtime.txt` lived in `backend/` but Render reads the version file from the service's Root Directory (`.`). Moved `backend/runtime.txt` → `runtime.txt`. Updated deployment README and story Dev Notes to flag this gotcha.
- 2026-04-22 — Second Render deploy failed (still Python 3.14, `uvicorn` exited 1 after 26 s). Root causes: (1) `runtime.txt` is a Heroku convention — Render silently ignores it at any path; Render uses `.python-version` or `PYTHON_VERSION` env var. (2) `backend/config.py` used deprecated pydantic v1-style `class Config` — risky with pydantic-settings 2.14.0 (released day before) + Python 3.14. Fixes: delete `runtime.txt`, add `.python-version` + `render.yaml`, migrate `config.py` to `SettingsConfigDict`.
- 2026-04-22 — Third Render deploy: Python 3.11 confirmed (cp311 wheels). Still exited 1 after ~21 s. Root cause: `openai` was never in `requirements.txt`. `backend/services/ai.py` imports `openai` at module level (top-level `from openai import ...`). Works locally because dev venv has it; Render installs only what is listed. The 21-second pattern = free-tier disk loading ~60 packages serially, failing at the `openai` import at the very end. Fix: add `openai>=1.0.0` to `requirements.txt`.
- 2026-04-25 — Dev agent: fixed mobile transformer crash (top-level `confidence.market_confidence` instead of legacy `valuation.confidence`); device retest pending.
- 2026-04-25 — Device validation pass (AirPods Max → $54, Logitech mouse → $12, both with live eBay sold-comp counts). Code review run; HIGH-3, HIGH-4, MEDIUM-5, MEDIUM-6, MEDIUM-7, LOW-8 resolved. Status → review; sprint-status synced.
- 2026-04-26 — Code-review cleanup: top-level confidence precedence corrected, Render bootstrap/env warnings added, validation green; status → done.
