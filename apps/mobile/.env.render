# Render backend URL — committed to document the Render endpoint; no secrets stored here.
# Copy this value into your local .env (or .env.local) to hit the deployed API.
# Set EXPO_PUBLIC_USE_MOCK=false in the same file for real-device tests.
EXPO_PUBLIC_API_URL=https://valuesnapapp.onrender.com
