# Render backend URL — committed to document the URL pattern; no secrets stored here.
# After deploying to Render, replace the placeholder below with your service URL from the dashboard.
# Copy to .env or .env.local and set EXPO_PUBLIC_USE_MOCK=false for real-device tests.
# EXPO_PUBLIC_API_URL=https://<your-service-name>.onrender.com
